import 'package:flutter/material.dart';

class CustomColors {
  // gunmental ---> bg color
  // charcoal ---> cards bg color (like barber card, input field (refer design))

  static const Color peelOrange = Color(0xFFfb9400);
  static const Color gunmetal = Color(0xFF181a20);
  static const Color charcoal = Color(0xFF1f222a);
  static const Color white = Color(0xFFffffff);
  static const Color transparent = Colors.transparent;
  static Color black = const Color(0xFF000000);
}
