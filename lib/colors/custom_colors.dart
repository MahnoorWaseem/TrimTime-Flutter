import 'package:flutter/material.dart';

class CustomColors {
  // gunmental ---> bg color
  // charcoal ---> cards bg color (like barber card, input field (refer design))

  static Color peelOrange = const Color(0xFFfb9400);
  static Color gunmetal = const Color(0xFF181a20);
  static Color charcoal = const Color(0xFF1f222a);
  static Color white = const Color(0xFFffffff);
  static Color transparent = Colors.transparent;
}
